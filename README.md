# 🧠 Telegram-бот для психолога

Умный бот для управления записями клиентов к психологу. Автоматизирует процесс записи, напоминания и управление расписанием.

## 📋 Описание проекта

Этот Telegram-бот разработан для оптимизации работы психолога с клиентами. Бот позволяет:
- **Клиентам**: записываться на консультации в удобное время, просматривать свои записи, переносить и отменять встречи
- **Психологу**: управлять рабочим расписанием, просматривать записи, получать уведомления и вручную добавлять клиентов

## ✨ Основные возможности

### Для клиентов:
- 📅 **Онлайн запись** — выбор услуги, даты и времени через интерактивное меню
- 🔔 **Автоматические напоминания** — уведомления за 24 часа и в день приёма
- 🔄 **Перенос записи** — возможность изменить дату/время существующей записи
- ❌ **Отмена записи** — простая отмена через бота
- 🗓 **Просмотр записей** — список всех активных записей с кнопками управления

### Для психолога:
- 🗓 **Управление расписанием** — настройка рабочих дней и часов по дням недели
- 🚫 **Блокировка слотов** — ручное закрытие времени (отпуск, личные дела)
- 📋 **Просмотр записей** — фильтрация по датам (сегодня, завтра, неделя, произвольная дата)
- ➕ **Ручная запись** — добавление клиентов, записавшихся по телефону
- 📊 **Ежедневный дайджест** — утренняя сводка всех приёмов на день
- 💬 **Уведомления** — информирование о подтверждениях и отменах клиентами

## 🛠 Технологический стек

- **Python 3.11+** — основной язык программирования
- **aiogram 3.4+** — асинхронный фреймворк для Telegram Bot API
- **SQLAlchemy 2.0+** — ORM для работы с базой данных
- **PostgreSQL** — реляционная база данных
- **APScheduler** — планировщик задач для напоминаний
- **asyncpg** — асинхронный драйвер PostgreSQL
- **python-dotenv** — управление переменными окружения

## 📁 Структура проекта

```
PsychologistShatayloBot/
├── bot.py                      # Точка входа, запуск бота
├── config.py                   # Конфигурация, переменные окружения
├── create_tables.py            # Утилита для создания таблиц БД
├── requirements.txt            # Зависимости проекта
│
├── database/                   # Работа с базой данных
│   ├── __init__.py
│   ├── models.py              # ORM-модели SQLAlchemy
│   └── session.py             # Управление сессиями БД
│
├── handlers/                   # Обработчики команд и событий
│   ├── client/                # Обработчики для клиентов
│   │   ├── booking.py         # Процесс записи (FSM)
│   │   ├── cancel.py          # Отмена записи
│   │   ├── menu.py            # Главное меню клиента
│   │   ├── reminders.py       # Подтверждение/отмена через кнопки
│   │   └── reschedule.py      # Перенос записи
│   │
│   └── psychologist/          # Обработчики для психолога
│       ├── menu.py            # Главное меню психолога
│       ├── records.py         # Просмотр записей
│       ├── schedule.py        # Управление недоступными слотами
│       └── work_hours.py      # Настройка рабочего расписания
│
├── keyboards/                  # Клавиатуры для взаимодействия
│   ├── inline.py              # Inline-клавиатуры (кнопки под сообщениями)
│   └── reply.py               # Reply-клавиатуры (постоянное меню)
│
├── services/                   # Бизнес-логика
│   ├── scheduler.py           # Планировщик напоминаний
│   └── slots.py               # Логика свободных слотов
│
├── states/                     # FSM-состояния
│   ├── client_states.py       # Состояния процесса записи клиента
│   └── psychologist_states.py # Состояния функций психолога
│
└── utils/                      # Вспомогательные утилиты
    └── decorators.py          # Декораторы (например, @psychologist_only)
```

## 🚀 Установка и запуск

### Предварительные требования

- Python 3.11 или выше
- PostgreSQL 12 или выше
- Telegram Bot Token (получить у [@BotFather](https://t.me/botfather))

### Шаг 1: Клонирование репозитория

```bash
git clone <URL-репозитория>
cd PsychologistShatayloBot
```

### Шаг 2: Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/macOS
source venv/bin/activate
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 4: Настройка базы данных

1. Создайте базу данных PostgreSQL:
```sql
CREATE DATABASE psychologist_bot_db;
```

2. Создайте файл `.env` в корневой директории проекта:
```env
# Токен Telegram-бота (получить у @BotFather)
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Telegram ID психолога (узнать у @userinfobot)
PSYCHOLOGIST_ID=123456789

# Параметры подключения к PostgreSQL
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=psychologist_bot_db
```

### Шаг 5: Создание таблиц в базе данных

```bash
python create_tables.py
```

### Шаг 6: Запуск бота

```bash
python bot.py
```

После успешного запуска вы увидите сообщение:
```
Запуск Telegram-бота психолога...
Бот успешно запущен и готов к работе!
```

## 📖 Примеры использования

### Для клиента

1. **Начать диалог с ботом**: `/start`
2. **Записаться на приём**:
   - Нажать кнопку "📅 Записаться"
   - Ввести ФИО и телефон (при первой записи)
   - Выбрать услугу (Консультация / Первая встреча / Супервизия)
   - Выбрать удобную дату из доступных
   - Выбрать время из свободных слотов
   - Подтвердить запись

3. **Просмотр записей**: Нажать "🗓 Мои записи"
   - Появится список активных записей
   - Доступны кнопки "Отменить" и "Перенести" для каждой записи

4. **Подтверждение записи**: Бот пришлёт напоминание — нажать "✅ Да" или "❌ Нет"

### Для психолога

1. **Открыть меню психолога**: `/psych`

2. **Настроить рабочее расписание**:
   - Нажать "🗰 Редактировать рабочее расписание"
   - Нажать "➕ Добавить / Изменить"
   - Выбрать день недели
   - Ввести время начала (например: `10:00`)
   - Ввести время окончания (например: `18:00`)

3. **Закрыть время вручную** (отпуск, личные дела):
   - Нажать "🗓 Указать недоступное время"
   - Ввести дату (формат: `ГГГГ-ММ-ДД`, например: `2025-11-15`)
   - Ввести время начала (формат: `ЧЧ:ММ`, например: `14:00`)
   - Ввести время окончания (например: `16:00`)

4. **Просмотр записей**:
   - Нажать "📋 Показать записи"
   - Выбрать период: Сегодня / Завтра / Неделя / Выбрать дату
   - Для каждой записи доступна кнопка "❌ Отменить"

5. **Ручная запись клиента**:
   - Нажать "🔎 Посмотреть свободные слоты"
   - Ввести дату (формат: `ДД.ММ.ГГГГ`, например: `05.11.2025`)
   - Выбрать свободное время
   - Ввести ФИО клиента
   - Ввести телефон клиента
   - Подтвердить запись (написать "Да")

## ⚙️ Конфигурация

### Переменные окружения (.env)

| Переменная | Описание | Обязательная | Пример |
|------------|----------|--------------|--------|
| `BOT_TOKEN` | Токен Telegram-бота | Да | `1234567890:ABC...` |
| `PSYCHOLOGIST_ID` | Telegram ID психолога | Да | `123456789` |
| `DB_USER` | Имя пользователя PostgreSQL | Нет (по умолчанию: postgres) | `postgres` |
| `DB_PASSWORD` | Пароль PostgreSQL | Нет (по умолчанию: postgres) | `your_password` |
| `DB_HOST` | Хост PostgreSQL | Нет (по умолчанию: localhost) | `localhost` |
| `DB_PORT` | Порт PostgreSQL | Нет (по умолчанию: 5432) | `5432` |
| `DB_NAME` | Имя базы данных | Нет (по умолчанию: psychologist_bot_db) | `psychologist_bot_db` |

### Настройка слотов записи

По умолчанию слоты создаются с интервалом **60 минут**. Чтобы изменить это:

1. Откройте файл `services/slots.py`
2. Найдите строку: `step = timedelta(minutes=60)`
3. Измените значение на нужное (например, `30` для получасовых слотов)

### Время отправки напоминаний

- **За 24 часа до приёма** — отправляется автоматически
- **Утреннее напоминание в день приёма** — отправляется в **7:30**
- **Ежедневный дайджест психологу** — отправляется в **7:30**

Чтобы изменить время, откройте `services/scheduler.py` и найдите:
```python
scheduler.add_job(
    send_daily_digest,
    args=[bot],
    trigger="cron",
    hour=7,
    minute=30
)
```

## 🗄 База данных

### Схема таблиц

#### clients (Клиенты)
- `id` — первичный ключ
- `full_name` — ФИО клиента
- `phone_number` — номер телефона
- `telegram_id` — Telegram ID (уникальный)
- `notes` — заметки психолога

#### appointments (Записи)
- `id` — первичный ключ
- `client_id` — внешний ключ на clients
- `date_time` — дата и время приёма
- `service` — тип услуги (consult/intro/supervision)
- `status` — статус (active/cancelled/completed)
- `confirmed` — подтверждено ли клиентом

#### work_schedule (Рабочее расписание)
- `id` — первичный ключ
- `weekday` — день недели (0=Пн, 6=Вс)
- `start_time` — время начала работы
- `end_time` — время окончания работы

#### unavailable_slots (Недоступные слоты)
- `id` — первичный ключ
- `date_time_start` — начало недоступного периода
- `date_time_end` — конец недоступного периода
- `reason` — причина недоступности

## 🤝 Разработка

### Стиль кода

Проект следует стандарту **PEP 8**. Основные правила:
- Отступы: 4 пробела
- Максимальная длина строки: 79 символов для кода, 72 для docstrings
- Имена переменных и функций: `snake_case`
- Имена классов: `PascalCase`
- Константы: `UPPER_CASE`

### Документирование

Все функции, классы и модули должны иметь docstrings на русском языке:

```python
def example_function(param: str) -> int:
    """
    Краткое описание функции.
    
    Подробное описание того, что делает функция,
    когда её использовать и какие особенности учесть.
    
    Args:
        param (str): Описание параметра
        
    Returns:
        int: Описание возвращаемого значения
        
    Raises:
        ValueError: Когда возникает исключение
        
    Example:
        >>> result = example_function("test")
        >>> print(result)
        42
    """
    pass
```

## 🐛 Решение проблем

### Бот не отвечает на команды
1. Проверьте правильность `BOT_TOKEN` в файле `.env`
2. Убедитесь, что бот запущен (`python bot.py`)
3. Проверьте логи на наличие ошибок

### Не работают напоминания
1. Убедитесь, что бот работает непрерывно
2. Проверьте системное время сервера
3. Убедитесь, что у клиента сохранён `telegram_id` в базе данных

### Нет свободных слотов
1. Проверьте рабочее расписание психолога (команда `/psych` → "Редактировать рабочее расписание")
2. Убедитесь, что не все слоты заняты или закрыты вручную
3. Проверьте, что выбранная дата не в прошлом

### Ошибки подключения к базе данных
1. Убедитесь, что PostgreSQL запущен
2. Проверьте правильность параметров подключения в `.env`
3. Проверьте, что база данных создана: `CREATE DATABASE psychologist_bot_db;`
4. Запустите создание таблиц: `python create_tables.py`

## 📝 Лицензия

Этот проект создан для образовательных целей и личного использования.

## 👤 Автор

Разработано для портфолио Python-разработчика.

## 📧 Контакты

Если у вас есть вопросы или предложения, создайте Issue в репозитории.

---

**Приятного использования! 🚀**
