# PsychologistShatayloBot

## Описание

Telegram-бот для автоматизации записи клиентов к психологу, управления расписанием, напоминаний и подтверждений. Поддерживает роли клиента и психолога, гибкое расписание, напоминания, подтверждение и отмену записей, а также просмотр статистики и управление рабочими часами.

**Новые функции:**
- Психолог может просматривать свободные слоты на выбранную дату и вручную записывать клиентов (например, по телефону или лично).
- Клиент, записанный вручную, получает уведомление о записи через Telegram (если есть Telegram ID).

---

## Возможности
- Онлайн-запись клиентов на консультации
- Управление расписанием и рабочими часами психолога
- Подтверждение и отмена записей
- Автоматические напоминания клиентам и психологу
- Просмотр записей по дате, неделе, периоду
- Гибкая работа с рабочими днями и недоступными слотами
- **Просмотр свободных слотов психологом и ручная запись клиента с уведомлением**

---

## Установка

1. Клонируйте репозиторий:
   ```bash
   git clone <URL-репозитория>
   cd PsychologistShatayloBot
   ```
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
3. Создайте файл `.env` и укажите переменные окружения (см. ниже).
4. Создайте таблицы в базе данных:
   ```bash
   python create_tables.py
   ```

---

## Переменные окружения (.env)

```
BOT_TOKEN=токен_вашего_бота
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=psychologist_bot_db
PSYCHOLOGIST_ID=123456789  # Telegram ID психолога
```

---

## Запуск бота

```bash
python bot.py
```

---

## Структура проекта

- `bot.py` — точка входа, запуск бота и регистрация хэндлеров
- `config.py` — конфигурация и переменные окружения
- `create_tables.py` — создание таблиц в БД
- `database/` — модели и сессия SQLAlchemy
- `handlers/` — хэндлеры для клиента и психолога
- `keyboards/` — inline и reply клавиатуры
- `services/` — бизнес-логика (слоты, планировщик)
- `states/` — FSM-состояния для aiogram
- `utils/` — вспомогательные функции и декораторы

---

## Основные команды

- `/start` — запуск бота (клиент)
- `/menu` — главное меню клиента
- `/psych` — главное меню психолога
- `/schedule` — расписание психолога
- `/my` — мои записи (клиент)
- **"🔎 Посмотреть свободные слоты"** — кнопка в меню психолога для просмотра свободных слотов и ручной записи клиента

---

## Поддержка и контакты

- Вопросы и предложения: [ваш email или Telegram]
- Разработчик: [ФИО психолога или команды]

---

## Лицензия

MIT License 